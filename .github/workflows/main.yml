name: Deploy to InfinityFree via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ ftpupload.net }}
          username: ${{ if0_39668451 }}
          password: ${{ 1Sln68EYt0a7n0 }}
          server-dir: /htdocs/
